#include <iostream>
#include <vector>
#include <stack>
#include <string>
#include <algorithm>
#include <cmath>
#include <cstdlib>
#include <cstring>
#include <sstream>
#include <map>
using namespace std;
int main()
{
    int n;
    vector<long long > x(n);
    vector<long long > y(n);
    for (int i = 0; i <x.size() ; ++i) {
        cin>>x[i];
    }
    for (int i = 0; i <x.size() ; ++i) {
        cin>>y[i];
    }

    return 0;
}
